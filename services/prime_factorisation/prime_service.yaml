apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: prime-fact
spec:
  template:
    spec:
      containers:
        - image: prime-fact:v1
          imagePullPolicy: Never
          ports:
            - containerPort: 8080
          env:
            - name: TARGET
              value: "Prime Factorisation"